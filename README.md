# AlwaysSpawn

A small [PowerNukkitX](https://github.com/PowerNukkitX/PowerNukkitX) plugin that always sends players to the default world's spawn point when spawning.

## Installation

Place `AlwaysSpawn.jar` in your server's `plugins` directory and restart the server. No configuration is required.
